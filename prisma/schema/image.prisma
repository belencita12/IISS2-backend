model Image {
    id Int @id @default(autoincrement())

    path        String @unique
    previewUrl  String
    originalUrl String

    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt
}
