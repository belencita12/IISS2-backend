model Species{
    id  Int @id @default(autoincrement())
    name    String
    races Race[]
    pets Pet[]
    createdAt   DateTime @default(now())  
    updatedAt   DateTime @updatedAt
    deletedAt   DateTime?
}